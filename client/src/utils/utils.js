export const paginate = () => {
  const pages = [];
  for (let i = 1; i <= 8; i++) {
    pages.push(i);
  }
  return pages;
};
export const getStars = () => {
  const stars = [];
  for (let i = 1; i <= 5; i++) {
    stars.push(i);
  }
  return stars;
};
